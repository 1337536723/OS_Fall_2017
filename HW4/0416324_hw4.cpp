#include <bits/stdc++.h>
using namespace std;
/**************************************************************************************
Specifics
Assume the TLB, page table, and physical memory is empty at the beginning.
• 2^4 entries in the TLB (Use LRU replacement Algorithm)
• 2^8 entries in the page table
• Page size of 2^8 bytes
• Frame size of 2^8 bytes
• 2^8 frames
• Physical memory of 65536 bytes (256 frames * 256-byte frame size)

**************************************************************************************/
struct content
{
    bool v; //valid bit
    int last_access_time;
}
void simulation(int tcase)
{
    // for(int i=0;i<)
}
int main(int argc, char const *argv[])
{
    FILE* fptr;
    fptr = fopen("address.txt","r")
    while(fptr)
    {
        cout<<"Fptr reads "<<fptr<<endl;
    }
    
    return 0;
}
